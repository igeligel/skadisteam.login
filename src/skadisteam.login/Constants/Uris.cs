using System;

namespace skadisteam.login.Constants
{
    internal class Uris
    {
        internal static Uri HelpSteampoweredSecureBase = new Uri("https://help.steampowered.com");
        internal static Uri SteamCommunityBase = new Uri(Resources.SteamCommmunityBase);
        internal static Uri SteamCommunitySecureBase = new Uri(Resources.SteamCommmunitySecureBase);
    }
}
